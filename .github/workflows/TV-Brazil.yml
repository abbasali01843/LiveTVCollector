name: TV-Brazil Update Files

on:
  schedule:
    - cron: '0 0,8,16 * * *'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: collector-main
  cancel-in-progress: false
jobs:
  update-files:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --upgrade pip requests
      - run: python BugsfreeMain/TV-Brazil.py
      - name: Commit and push changes
        run: |
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add LiveTV/Brazil/LiveTV.m3u LiveTV/Brazil/LiveTV.txt LiveTV/Brazil/LiveTV.json LiveTV/Brazil/LiveTV
          git commit -m "TV-Brazil Update files - $(date -u '+%Y-%m-%d %H:%M:%S UTC')" || exit 0
          git pull --rebase origin main
          git push
