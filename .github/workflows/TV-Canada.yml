name: TV-Canada Update Files

on:
  schedule:
    - cron: '0 0,8,16 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: collector-main
  cancel-in-progress: false

jobs:
  update-files:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --upgrade pip requests
      - run: python BugsfreeMain/TV-Canada.py
      - name: Commit and push changes
        run: |
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add LiveTV/Canada/LiveTV.m3u LiveTV/Canada/LiveTV.txt LiveTV/Canada/LiveTV.json LiveTV/Canada/LiveTV
          git commit -m "Update Canada TV files" || exit 0
          git push
